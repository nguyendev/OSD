@model IEnumerable<QuanLyNhaHang.Models.LOAISUCO>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MaBoPhanXuLy)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MaLoaiSuCo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TenLoaiSuCo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MaBoPhanXuLy)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MaLoaiSuCo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TenLoaiSuCo)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
