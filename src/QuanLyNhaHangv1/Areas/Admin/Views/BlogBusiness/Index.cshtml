@model IEnumerable<QuanLyNhaHangv1.Models.BlogBusiness>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-action="UpdateBusiness">Cap nhat</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BussinessId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BusinessName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BussinessId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BusinessName)
            </td>
            <td>
                @*<a asp-action="Edit" asp-route-id="@item.BussinessId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.BussinessId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.BussinessId">Delete</a>*@
            </td>
        </tr>
}
    </tbody>
</table>
